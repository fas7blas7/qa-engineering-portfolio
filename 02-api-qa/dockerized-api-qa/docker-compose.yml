version: "3.8"

services:
  api:
    build: ./api
    container_name: api
    ports:
      - "3000:3000"

  newman:
    image: postman/newman
    container_name: newman
    depends_on:
      - api
    volumes:
      - ./newman:/etc/newman
      - ./reports:/etc/reports
    command: >
      run /etc/newman/collections/reqres.json
      -e /etc/newman/environments/reqres.env.json
      -r cli